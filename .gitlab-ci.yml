stages:
  - deploy

pages:
  image: node:latest
  stage: deploy
  script:
    - npm config set registry https://registry.npm.taobao.org
    - npm install -f
    - ./node_modules/.bin/gatsby build --prefix-paths
  artifacts:
    paths:
      - public
  only:
    - main
