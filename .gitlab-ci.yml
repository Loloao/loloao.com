stages:
  - deploy

pages:
  image: node:latest
  stage: deploy
  script:
    - mkdir node_modules
    - cp -r ./gatsby/* ./node_modules/gatsby
    - npm install -f
    - ./node_modules/.bin/gatsby build --prefix-paths
  artifacts:
    paths:
      - public
  only:
    - main
